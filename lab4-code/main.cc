#include "Run.h"
#include "Result.h"
//extern RUN
int main(){
    system("mkdir output");
    freopen((outpath1).c_str(), "w", stdout);
    printf("file1 , file2\n");
    fclose(stdout);
    fflush(stdout);
    freopen((outpath2).c_str(), "w", stdout);
    printf("file1 , file2\n");
    fclose(stdout);
    fflush(stdout);
    Run();
    return 0;
}